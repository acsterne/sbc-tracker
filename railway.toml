[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app"
healthcheckPath = "/"
restartPolicyType = "on_failure"

[[crons]]
name = "fetch-sbc"
schedule = "0 6 * * 1"   # Every Monday at 6am UTC (quarterly filings are staggered)
command = "python3 fetch_sbc.py"
